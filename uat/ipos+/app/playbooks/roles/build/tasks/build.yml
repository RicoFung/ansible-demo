---
# get start build time
- name: ====== 2 ======== get start build time
  shell: date +%Y%m%d_%H%M%S
  register: build_time

- name: ====== 3 ======== npm build
  shell: source ~/.bash_profile && npm run build
  args:
    chdir: "{{GV_GIT_DIR}}"